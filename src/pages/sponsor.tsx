import React from 'react'
import styled from 'styled-components'

const Sponsor = () => {
  return <Container>Sponsor</Container>
}

const Container = styled.div`
  display: grid;
`

export default Sponsor
