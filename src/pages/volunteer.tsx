import React from 'react'
import styled from 'styled-components'

const volunteer = () => {
  return <Container>volunteer</Container>
}

const Container = styled.div`
  display: grid;
`

export default volunteer
