import React from 'react'
import styled from 'styled-components'

const Adopt = () => {
  return <Container>Adopt</Container>
}

const Container = styled.div`
  display: grid;
`

export default Adopt
